# Phase 2 contrastive-encoder training pipeline.
# Apple Silicon / MPS target. NO bitsandbytes (CUDA-only; 4-bit path is a CUDA-only seam).
# Pinned to versions verified to import + run cleanly on Python 3.13 / macOS arm64 / MPS.
torch==2.12.0
transformers==5.9.0
peft==0.19.1
accelerate==1.13.0
numpy==2.4.6
PyYAML==6.0.3
tqdm==4.67.3
matplotlib==3.10.9
pytest==9.0.3
